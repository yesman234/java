import java.util.ArrayList;
public class Lottery {

	public static void main(String[] args) {
		ArrayList<Integer> guess = new ArrayList<Integer>();

		int secretNumber = ((int)Math.floor(Math.random()*10));
		int userNumbers = (9);
		
		int i =(0);
		
		for ( i = (0); i<10; ++i){
			userNumbers = ((int)Math.floor(Math.random()*10));
			if(userNumbers==secretNumber) {
				System.out.println("your a winner");
				break;
			}
		}
		
System.out.println("Took" + (i +1) + "number of tries");

	}}
